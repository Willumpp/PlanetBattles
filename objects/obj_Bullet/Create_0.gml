player = undefined
damage = 1;
colour = c_red;

velocity = [0,0] //Velocity isnt used for movement in this object, it is here for parity between other bullets

parTypeDestroy = part_type_create();

shake_factor = 0.5;
shake_length = 1;

xdraw_scale = image_xscale;
ydraw_scale = image_xscale;